# Web Frameworks (FastAPI Backend)
fastapi
uvicorn
python-multipart  # required by FastAPI for UploadFile / File(...)

# AI Frameworks (RAG & Qwen Orchestration)
llama-index
llama-index-llms-openai          # actually imported by generation_service
llama-index-embeddings-huggingface

# Transcription & Audio/Video Processing
faster-whisper
scenedetect[opencv]              # PySceneDetect for keyframe extraction (Phase 1)
opencv-python
pydantic
# Note: ffmpeg + ffprobe must be available on PATH for processing/media_demux.py

# OCR + VLM cascade (Phase 4) — OPTIONAL, only needed when OCR_ENABLED=true
# PaddleOCR runs on CPU; Qwen-VL goes through 9router so no local GPU is needed.
# paddleocr
# paddlepaddle

# Environment & Utilities
python-dotenv
aiofiles
httpx
faiss-cpu
